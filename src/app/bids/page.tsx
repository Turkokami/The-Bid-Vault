import { PlaceholderPanel } from "@/components/placeholder-panel";

export default function BidsPage() {
  return (
    <PlaceholderPanel
      eyebrow="Winning bids"
      title="Historical award data is staged for this view."
      body="This route is ready for the historical winning bids table, pricing patterns, and competitor history once the contract detail flows are in place."
    />
  );
}
